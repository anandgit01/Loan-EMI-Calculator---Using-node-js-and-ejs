<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Loan Calculator</title>
    <style>
        body {
          font-family: Arial, sans-serif;
          margin: 0;
          padding: 0;
        }
        h1 {
          background-color: #0077cc;
          color: #ffffff;
          margin: 0;
          padding: 20px;
          text-align: center;
        }
        form {
          background-color: #f2f2f2;
          border: 1px solid #cccccc;
          border-radius: 5px;
          margin: 20px auto;
          max-width: 400px;
          padding: 20px;
        }
        form div {
          margin-bottom: 10px;
        }
        label {
          display: inline-block;
          font-weight: bold;
          margin-right: 10px;
          width: 150px;
        }
        input[type="number"] {
          border: 1px solid #cccccc;
          border-radius: 3px;
          font-size: 16px;
          padding: 5px;
          width: 200px;
        }
        button[type="submit"] {
          background-color: #0077cc;
          border: none;
          border-radius: 3px;
          color: #ffffff;
          cursor: pointer;
          font-size: 16px;
          padding: 10px;
          width: 100%;
        }
        button[type="submit"]:hover {
          background-color: #005fa3;
        }
        h2 {
          background-color: #008cba;
          color: #ffffff;
          margin: 20px 0 0;
          padding: 10px;
          text-align: center;
        }
      </style>
      
  </head>
  <body>
    <h1>Loan Calculator</h1>
    <% if (monthlyPayment) { %>
      <h2>Monthly Payment: <%= monthlyPayment.toFixed(2) %></h2>
    <% } %>
    <form action="/" method="post">
      <div>
        <label for="loanAmount">Loan Amount:</label>
        <input type="number" step="0.01" name="loanAmount" id="loanAmount" required>
      </div>
      <div>
        <label for="interestRate">Interest Rate (%):</label>
        <input type="number" step="0.01" name="interestRate" id="interestRate" required>
      </div>
      <div>
        <label for="loanTermYears">Loan Term (years):</label>
        <input type="number" step="1" name="loanTermYears" id="loanTermYears" required>
      </div>
      <button type="submit">Calculate</button>
    </form>
  </body>
</html>
